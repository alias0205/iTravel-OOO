import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    logoBlock: {
        alignItems: 'center',
        marginBottom: 30,
    },
    logoImage: {
        width: 168,
        height: 64,
    },
});

export { styles };
export default styles;
